//seTimeout : this method executes a function, after waiting a specified number of milliseconds.
//setInterval : this method repeats a given function at every given time-interval.

setTimeout(() => {
  alert("Hello setTimeout");
}, 3000);

setInterval(() => {
  alert("Hello setInterval");
}, 3000);
